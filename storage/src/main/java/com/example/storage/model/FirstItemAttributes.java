package com.example.storage.model;

import javax.persistence.Id;

public enum FirstItemAttributes {
    loginEmail,
    id,
    oneTimePassword,
    recoveryCode,
    firstName,
    lastName,
    department,
    location
}